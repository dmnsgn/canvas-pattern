/**
 * @typedef {"repeat" | "repeat-x" | "repeat-y" | "no-repeat"} CanvasPatternRepetition
 */

/**
 * @typedef {Object} CanvasPatternOptions Options to draw. All optional.
 * @property {CanvasPattern} [pattern] The CanvasPattern to draw. Created and returned by the function for reuse.
 * @property {CanvasPatternRepetition} [repetition] A DOMString indicating how to repeat the pattern's image.
 * @property {number} [x]
 * @property {number} [y]
 * @property {number} [width]
 * @property {number} [height]
 */

export {};
